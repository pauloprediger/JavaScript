const  forma = 'quadrado';
const altura = 10;
const comprimento = 14;

if (forma === 'quadrado') {
    area = (altura * comprimento) / 2;
} else { 
    'é uma forma geométrica';

    let area;
    console.log(area);
}